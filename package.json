{
  "name": "vexo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y serve",
    "tauri": "tauri",
    "build": "node copy-dist.js && tauri build",
    "build:web": "echo 'Vexo is a static site — no build needed'"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-fs": "^2.5.1",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
